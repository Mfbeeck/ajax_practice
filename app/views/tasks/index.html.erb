<p id="notice"><%= notice %></p>

<h1>New Task</h1>

<%= render 'form' %>

<%= link_to 'Back', tasks_path %>

<h1>Listing Tasks</h1>

    <% @tasks.each do |task| %>
      <%= render task %>
  
    <% end %>

